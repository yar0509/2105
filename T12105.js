/*1. Напишите функцvar array = [1,2,3];
console.log(array[0]);

function copyArr() {
var clone = {};
for(var arr in array){
    if(array.hasOwnProperty(arr)){
        clone(arr)=array(arr);
        }
    }
}
console.log(copyArr)ию copyArr(arr),
которая копирует массив не изменяя оригинал.*/



/*я пытался создать функцию,
которая будет клонировать массив
но чё-то получилась какая-то ерундень...*/